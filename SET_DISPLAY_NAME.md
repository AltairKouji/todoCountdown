# 👤 设置用户显示名称（中文名）

应用底部会显示当前登录用户的名称。默认显示邮箱前缀（如 `dad`），但您可以设置中文显示名称。

## 🎯 方法一：创建用户时设置（推荐）

在 Supabase Dashboard 创建用户时：

1. **Authentication** → **Users** → **Add user**
2. 填写基本信息：
   ```
   Email: dad@family.local
   Password: ******
   Auto Confirm User: ✅
   ```
3. 点击 **Additional user metadata** 展开
4. 在 JSON 编辑器中添加：
   ```json
   {
     "display_name": "爸爸"
   }
   ```
5. 点击 **Create user**

## 🔧 方法二：编辑现有用户

为已创建的用户添加显示名称：

1. **Authentication** → **Users**
2. 找到要修改的用户，点击进入详情
3. 找到 **User Metadata** 部分
4. 点击 **Edit** 按钮
5. 修改 JSON 为：
   ```json
   {
     "display_name": "妈妈"
   }
   ```
6. 点击 **Save**

## 📋 推荐的用户配置

```json
// 爸爸
{
  "email": "dad@family.local",
  "user_metadata": {
    "display_name": "爸爸"
  }
}

// 妈妈
{
  "email": "mom@family.local",
  "user_metadata": {
    "display_name": "妈妈"
  }
}

// 儿子
{
  "email": "son@family.local",
  "user_metadata": {
    "display_name": "小明"
  }
}

// 女儿
{
  "email": "daughter@family.local",
  "user_metadata": {
    "display_name": "小红"
  }
}
```

## ✅ 效果

设置后，应用底部显示：

- 有 display_name：`已登录: 爸爸`
- 无 display_name：`已登录: dad`

## 🔄 生效时间

- 新创建的用户：立即生效
- 修改现有用户：需要**重新登录**才能看到新名称

## 💡 提示

- display_name 可以是任何文本（中文、英文、emoji 都可以）
- 不同用户可以使用相同的 display_name（邮箱必须唯一）
- 如果不设置，会自动显示邮箱的 `@` 前面部分

---

**示例用户配置（完整）：**

| 邮箱 | 密码 | Display Name | 底部显示 |
|------|------|--------------|---------|
| dad@family.local | password123 | 爸爸 | 已登录: 爸爸 |
| mom@family.local | password456 | 妈妈 | 已登录: 妈妈 |
| son@family.local | password789 | 小明 | 已登录: 小明 |
| daughter@family.local | password000 | （不设置） | 已登录: daughter |
